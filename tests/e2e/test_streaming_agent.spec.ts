// PRD-required filename. The actual e2e implementation lives in
// `tests/e2e/test_streaming_agent.py` because this project's e2e harness
// is pytest-playwright (Python), not @playwright/test (TS). See that file
// for the streaming-agent assertions.
